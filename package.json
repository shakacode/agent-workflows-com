{
  "name": "agent-workflows-com",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check:adoption-ladder": "node scripts/check-adoption-ladder.mjs",
    "check:links": "node scripts/check-links.mjs",
    "test": "npm run build && npm run check:adoption-ladder && npm run check:links"
  },
  "dependencies": {
    "@fontsource-variable/fraunces": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@fontsource/ibm-plex-sans": "^5.3.0",
    "astro": "^7.1.6"
  }
}
