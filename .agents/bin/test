#!/usr/bin/env bash
# Generated by shakacode/agent-workflows agent-workflow-seam-doctor --init.
# Extended for issue #9: builds the site, then runs the offline
# homepage-adoption-ladder drift check against the built output.
set -euo pipefail
cd "$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"
exec npm test -- "$@"
