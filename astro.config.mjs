import { defineConfig } from 'astro/config';

// Canonical site URL. Update if a dedicated domain is chosen over the
// agent-workflows.shakacode.com subdomain (see README → Deploy).
export default defineConfig({
  site: 'https://agent-workflows.shakacode.com',
});
