---
// How the work moves: untrusted GitHub work passes a security gate (amber),
// flows through five stages, all backed by shared coordination.
//
// Every visible label here is deliberately plain English. This diagram sits in
// the hero, above the fold, so it is the first thing a visitor reads; the
// project's own vocabulary (skill names, coordination internals) is introduced
// further down the page under #how and #skills, never here.
//
// The five stages are the plain-language mirror of the #how list in
// src/pages/index.astro, position for position: plan-pr-batch -> plan,
// triage -> split, pr-batch -> run, adversarial-pr-review -> review,
// post-merge-audit -> audit. Keep the two lists in the same order; a reader
// scrolling from the hero to #how reads the numbering 1:1.
//
// Plain HTML + CSS, not a fixed-viewBox SVG. This diagram lives in the hero's
// right-hand panel, which is only ~480px wide at desktop and ~236px on a small
// phone; a `viewBox="0 0 960 …"` scaled to fit shrank its own type to 3-6px
// there and pushed the gate caption outside its box. Real text reflows and
// keeps a real font size at every width.
---
<figure class="lifecycle">
  <div
    class="lc-flow"
    role="img"
    aria-label="How the work moves: anything arriving from GitHub issues and pull requests passes a security gate, where untrusted input never becomes instructions, then flows through five stages — plan, split, run, review, and audit — with shared coordination, so parallel agents never collide."
  >
    <p class="lc-source">from github issues &amp; prs</p>

    <div class="lc-gate">
      <span class="lc-gate-name">security gate</span>
      <span class="lc-gate-sub">untrusted input never becomes instructions</span>
    </div>

    <span class="lc-arrow" aria-hidden="true">&#8595;</span>

    <div class="lc-engine">
      <ol class="lc-stages">
        <li><span class="lc-step">01</span><span class="lc-stage">plan</span></li>
        <li><span class="lc-step">02</span><span class="lc-stage">split</span></li>
        <li><span class="lc-step">03</span><span class="lc-stage">run</span></li>
        <li><span class="lc-step">04</span><span class="lc-stage">review</span></li>
        <li><span class="lc-step">05</span><span class="lc-stage">audit</span></li>
      </ol>
      <p class="lc-rail">
        <span class="lc-rail-lead">backed by</span>
        shared coordination, so parallel agents never collide
      </p>
    </div>
  </div>
</figure>

<style>
  .lifecycle { margin: 0; }

  .lc-flow { display: grid; gap: 10px; }

  /* the input: a label for what enters the gate, not a node */
  .lc-source {
    margin: 0;
    color: var(--ink-dim);
    font-family: var(--font-mono);
    font-size: 0.78rem;
    letter-spacing: 0.14em;
    line-height: 1.4;
    text-transform: uppercase;
  }

  /* amber is reserved for the gate motif — it stays the loudest element here */
  .lc-gate {
    display: grid;
    gap: 3px;
    padding: 13px 16px;
    border: 1px solid color-mix(in srgb, var(--gate) 62%, transparent);
    border-radius: 12px;
    background: color-mix(in srgb, var(--gate) 13%, var(--surface));
  }
  .lc-gate-name {
    display: flex;
    align-items: center;
    gap: 0.55em;
    color: var(--gate-ink);
    font-family: var(--font-body);
    font-size: 1rem;
    font-weight: 600;
    line-height: 1.3;
  }
  .lc-gate-name::before {
    content: "";
    flex: none;
    width: 8px; height: 8px;
    border-radius: 2px;
    background: var(--gate);
  }
  .lc-gate-sub {
    color: var(--gate-ink);
    font-family: var(--font-mono);
    font-size: 0.82rem;
    line-height: 1.45;
    overflow-wrap: break-word;
  }

  .lc-arrow {
    justify-self: center;
    color: var(--ink-dim);
    font-family: var(--font-mono);
    font-size: 1.15rem;
    line-height: 1;
  }

  /* the five stages sit inside the coordination box: containment is what says
     "shared coordination is what these run on" now that the dashed leader
     lines are gone. */
  .lc-engine {
    display: grid;
    gap: 11px;
    padding: 13px;
    border: 1px solid var(--line-strong);
    border-radius: 14px;
    background: color-mix(in srgb, var(--surface) 55%, transparent);
  }

  .lc-stages {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin: 0;
    padding: 0;
    list-style: none;
  }
  /* flex-grow means the last (short) row stretches to fill instead of leaving
     a ragged hole — 5 across when there is room, 3+2 in the hero panel,
     2+2+1 on a phone. */
  .lc-stages li {
    display: grid;
    gap: 1px;
    flex: 1 1 112px;
    padding: 8px 12px 9px;
    border: 1px solid color-mix(in srgb, var(--brand) 42%, transparent);
    border-radius: 10px;
    background: var(--surface);
  }
  .lc-step {
    color: var(--brand-ink);
    font-family: var(--font-mono);
    font-size: 0.78rem;
    letter-spacing: 0.1em;
    line-height: 1.3;
  }
  .lc-stage {
    color: var(--ink);
    font-family: var(--font-body);
    font-size: 0.95rem;
    font-weight: 500;
    line-height: 1.3;
    overflow-wrap: break-word;
  }

  .lc-rail {
    margin: 0;
    padding-top: 10px;
    border-top: 1px dashed var(--line-strong);
    color: var(--ink-dim);
    font-family: var(--font-mono);
    font-size: 0.8rem;
    line-height: 1.5;
  }
  .lc-rail-lead {
    color: var(--ink-dim);
    letter-spacing: 0.16em;
    text-transform: uppercase;
    margin-right: 0.55em;
  }

  @media (max-width: 25em) {
    .lc-stages li { flex-basis: 92px; }
  }
</style>
