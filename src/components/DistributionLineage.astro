<figure class="lineage">
  <div
    class="lineage-flow"
    role="img"
    aria-label="An Upstream Release becomes a Derived Distribution with explicit patches, which produces a verified installation."
  >
    <div class="lineage-node">
      <span class="kind">Upstream Release</span>
      <strong>reviewed baseline</strong>
    </div>
    <span class="arrow" aria-hidden="true">→</span>
    <div class="lineage-node derived">
      <span class="kind">Derived Distribution</span>
      <strong>baseline + explicit patches</strong>
    </div>
    <span class="arrow" aria-hidden="true">→</span>
    <div class="lineage-node">
      <span class="kind">Consumer Repo</span>
      <strong>verified installation</strong>
    </div>
  </div>
  <figcaption>Upstream release → Derived distribution + explicit patches → Verified installation</figcaption>
</figure>

<style>
  .lineage { margin: 0; }
  .lineage-flow { display: grid; grid-template-columns: 1fr auto 1.2fr auto 1fr; gap: 14px; align-items: stretch; }
  .lineage-node {
    display: grid; gap: 5px; align-content: center; min-height: 104px; padding: 18px;
    background: var(--surface); border: 1px solid var(--line-strong); border-radius: 12px;
  }
  .lineage-node.derived {
    border-color: color-mix(in srgb, var(--brand) 55%, transparent);
    background: color-mix(in srgb, var(--brand) 7%, var(--surface));
  }
  .kind { color: var(--brand-ink); font-family: var(--font-mono); font-size: 0.72rem; letter-spacing: 0.08em; text-transform: uppercase; }
  strong { color: var(--ink); font-family: var(--font-body); font-size: 0.95rem; font-weight: 500; line-height: 1.35; }
  .arrow { align-self: center; color: var(--ink-faint); font-family: var(--font-mono); font-size: 1.25rem; }
  figcaption { margin-top: 14px; color: var(--ink-dim); font-family: var(--font-mono); font-size: 0.76rem; }

  @media (max-width: 720px) {
    .lineage-flow { grid-template-columns: 1fr; }
    .lineage-node { min-height: auto; }
    .arrow { transform: rotate(90deg); justify-self: center; line-height: 1; }
  }
</style>
